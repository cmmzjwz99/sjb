<div class="container-fluid">
  <h3 class="page-title">推荐人</h3>
  <div class="row">
    <div class="col-md-12">
      <!-- BORDERED TABLE -->
      <div class="panel">
        <div style="padding: 20px 25px">
          <form>
            <input type="text" placeholder="账号" class="form-control index-form-control" name="name" value="<%=params[:name]%>">
            <button class="btn btn-primary" style="margin-left: 3px">查询</button>
            <!--a type="button" class="btn btn-default" href="">添加代理</a-->
          </form>
        </div>
        <div class="panel-body" style="padding-top: 0">
          <table class="table table-bordered">
            <thead>
            <tr>
              <th>ID</th>
              <th>账号</th>
              <th>推荐人数</th>
              <th>有效流水</th>
              <th>可提现金额</th>
              <th>已提现金额</th>
            </tr>
            </thead>
            <tbody>
            <% @users.each do |ele| %>
                <tr>
                  <td><%= ele.id %></td>
                  <td><%= ele.login %></td>
                  <td><%= User.where(referee:ele.referee,father_id:ele.id).count %></td>
                  <!--<td><%= Payment.where({user:User.where(referee:ele.referee,father_id:ele.id),status:1,payment_type: 1}).sum(:balance)%></td>-->
                  <td><%= ele.effective_journal %></td>
                  <td><%= ele.effective_journal*(((Setting.where(category:'rebate')[0] || Setting.new(category:'rebate')).val).to_f) %></td>
                  <td><%= ele.rebate %></td>
                </tr>
            <% end %>
            </tbody>
          </table>
          <!--<table class="table table-bordered" style="margin-bottom: 0">-->
            <!--<thead>-->
            <!--<tr>-->
              <!--<th>#</th>-->
              <!--<th>账号</th>-->
              <!--<th>充值总额</th>-->
              <!--<th>提现总额</th>-->
              <!--<th>操作</th>-->
            <!--</tr>-->
            <!--</thead>-->
            <!--<tbody>-->
              <!--<%@users.each do |ele|%>-->
                <!--<tr>-->
                  <!--<td><%=ele.id%></td>-->
                  <!--<td><%=ele.login%></td>-->
                  <!--<td><%= Payment.where({user:User.where(father_id:ele.id),status:1,payment_type: 1}).sum(:balance) %></td>-->
                  <!--<td><%= Payment.where({user:User.where(father_id:ele.id),status:1,payment_type: 0}).sum(:balance) %></td>-->
                  <!--<td><a href="<%= agent_agent_path(ele) %>">详情</a></td>-->
                <!--</tr>-->
              <!--<%end%>-->
            <!--</tbody>-->
          <!--</table>-->

        </div>
      </div>
      <!-- END BORDERED TABLE -->
    </div>
  </div>
</div>